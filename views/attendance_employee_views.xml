<odoo>
    <!-- Tree View -->
    <record id="view_attendance_employee_tree" model="ir.ui.view">
        <field name="name">attendance.employee.tree</field>
        <field name="model">attendance.employee</field>
        <field name="arch" type="xml">
            <tree>
                <field name="employee_id"/>
                <field name="check_in"/>
                <field name="check_out"/>
                <field name="duration"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_attendance_employee_form" model="ir.ui.view">
        <field name="name">attendance.employee.form</field>
        <field name="model">attendance.employee</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="employee_id"/>
                        <field name="check_in"/>
                        <field name="check_out"/>
                        <field name="duration" readonly="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
